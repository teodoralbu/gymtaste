---
phase: 11-nav-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/BottomNav.tsx
autonomous: false
requirements:
  - NAV-01
must_haves:
  truths:
    - "Home tab (bar chart icon, /leaderboard) appears first in the bottom nav"
    - "Feed tab (house icon, /) appears second in the bottom nav"
    - "Navigating to /leaderboard highlights Home, not Feed"
    - "Navigating to / highlights Feed, not Home"
    - "Center spacer, Alerts, Profile, and Rate FAB are unchanged"
    - "Desktop Navbar is unchanged"
  artifacts:
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Reordered bottom nav with renamed tabs and corrected active state variables"
      contains: "feedActive"
  key_links:
    - from: "feedActive variable"
      to: "Feed tab JSX block"
      via: "tabStyle(feedActive), labelStyle(feedActive), {feedActive && <span .../>}"
      pattern: "feedActive"
    - from: "homeActive variable"
      to: "Home tab JSX block"
      via: "tabStyle(homeActive), labelStyle(homeActive), {homeActive && <span .../>}"
      pattern: "homeActive.*leaderboard"
---

<objective>
Rename and reorder the first two bottom nav tabs. "Top" (bar chart, /leaderboard) becomes "Home" and moves to position 1. "Home" (house, /) becomes "Feed" and moves to position 2. Active state variables are renamed to match the new semantics. No routes, icons, content, or other nav elements change.

Purpose: Tab labels and positions should match user intent — the leaderboard is the primary destination ("Home"), the feed is secondary ("Feed").
Output: A single edited file — `src/components/layout/BottomNav.tsx` — with the correct tab order, labels, and active state variable names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current BottomNav.tsx active state variables (lines 13-17): -->
```typescript
const homeActive    = pathname === '/'
const rateActive    = pathname.startsWith('/rate')
const topActive     = pathname.startsWith('/leaderboard')
const notifActive   = pathname.startsWith('/notifications')
const profileActive = pathname.startsWith('/users') || pathname.startsWith('/settings') || pathname === '/login' || pathname === '/signup'
```

<!-- Current JSX tab order (inside the <nav> flex container): -->
<!-- 1. Line 67:  <Link href="/">          label="Home"  icon=house      active=homeActive -->
<!-- 2. Line 77:  <Link href="/leaderboard"> label="Top"   icon=bar-chart  active=topActive -->
<!-- 3. Line 88:  <div style={{flex:1}} aria-hidden="true" />  ← CENTER SPACER — DO NOT MOVE -->
<!-- 4. Line 91:  <Link href="/notifications">  label="Alerts"  active=notifActive -->
<!-- 5. Line 125: <Link href={profileHref}>     label="Profile" active=profileActive -->
<!-- 6. Line 135: <Link href="/rate"> (absolute positioned FAB — untouched) -->

<!-- tabStyle(active) and labelStyle(active) are helper functions already defined.      -->
<!-- Active indicator: {active && <span style={{position:'absolute', top:'6px',...}} />} -->
<!-- strokeWidth on the SVG also uses the active boolean: strokeWidth={active ? 2.5 : 2} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename active state variables and reorder JSX tabs</name>
  <files>src/components/layout/BottomNav.tsx</files>
  <action>
Make the following targeted edits to `src/components/layout/BottomNav.tsx`. Do them in order so references stay consistent.

**Step A — Rename active state variables (lines 13-15)**

Replace:
```typescript
const homeActive    = pathname === '/'
const rateActive    = pathname.startsWith('/rate')
const topActive     = pathname.startsWith('/leaderboard')
```

With:
```typescript
const feedActive    = pathname === '/'
const rateActive    = pathname.startsWith('/rate')
const homeActive    = pathname.startsWith('/leaderboard')
```

`notifActive` and `profileActive` on lines 16-17 are unchanged.

**Step B — Reorder the two tab JSX blocks**

The first two `<Link>` blocks (the "Home" block at line 67 and the "Top Rated" block at line 77) must be swapped so that /leaderboard comes first. The center spacer `<div style={{ flex: 1 }} aria-hidden="true" />` must remain in position 3 — do not move it.

Replace the existing first two `<Link>` blocks (and their comments) with:

```tsx
      {/* Home */}
      <Link href="/leaderboard" style={tabStyle(homeActive)}>
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={homeActive ? 2.5 : 2} strokeLinecap="round" strokeLinejoin="round">
          <polyline points="18 20 18 10" />
          <polyline points="12 20 12 4" />
          <polyline points="6 20 6 14" />
        </svg>
        <span style={labelStyle(homeActive)}>Home</span>
        {homeActive && <span style={{ position: 'absolute', top: '6px', left: '50%', transform: 'translateX(-50%)', width: '16px', height: '2px', borderRadius: '999px', backgroundColor: 'var(--accent)' }} />}
      </Link>

      {/* Feed */}
      <Link href="/" style={tabStyle(feedActive)}>
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={feedActive ? 2.5 : 2} strokeLinecap="round" strokeLinejoin="round">
          <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
          <polyline points="9 22 9 12 15 12 15 22" />
        </svg>
        <span style={labelStyle(feedActive)}>Feed</span>
        {feedActive && <span style={{ position: 'absolute', top: '6px', left: '50%', transform: 'translateX(-50%)', width: '16px', height: '2px', borderRadius: '999px', backgroundColor: 'var(--accent)' }} />}
      </Link>
```

**Step C — Post-edit sanity check**

After saving, verify there are zero occurrences of `topActive` remaining in the file. The old variable name must be fully replaced. Also confirm `homeActive` now binds to `/leaderboard` (not `/`) and `feedActive` binds to `/`.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
TypeScript reports zero errors for BottomNav.tsx. The file contains `feedActive`, `homeActive` (bound to `/leaderboard`), and zero occurrences of `topActive`. The /leaderboard Link block appears before the / Link block in the JSX. The center spacer div is still in position 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — nav order and active states</name>
  <what-built>Reordered and relabeled bottom nav: Home (bar chart, /leaderboard) is now tab 1, Feed (house, /) is tab 2. Active state variables renamed to match.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the app on a mobile viewport (or DevTools Device Toolbar — any narrow viewport where the bottom nav is visible).
2. Check tab order left-to-right: **Home | Feed | [Rate FAB] | Alerts | Profile**
3. Navigate to `/leaderboard` — confirm the **Home** tab highlights (accent color, bold label, underline indicator). Feed tab must be inactive.
4. Navigate to `/` — confirm the **Feed** tab highlights. Home tab must be inactive.
5. Navigate to `/rate` — confirm both Home and Feed are inactive; the Rate FAB reflects active state.
6. Navigate to `/notifications` — confirm Alerts tab highlights; Home and Feed inactive.
7. Switch to a desktop/wide viewport — confirm the desktop Navbar is visually unchanged (still shows "Top Rated" link, etc.).
8. Run `npm run build` and confirm zero build errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issue observed.</resume-signal>
  <action>Human reviews the running app per steps in &lt;how-to-verify&gt; above.</action>
  <verify>User confirms all 8 steps pass and types "approved".</verify>
  <done>User has approved the nav order and active state behaviour on device.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with zero errors (`npx tsc --noEmit`)
- `topActive` does not appear anywhere in `src/components/layout/BottomNav.tsx`
- `homeActive` is bound to `pathname.startsWith('/leaderboard')`
- `feedActive` is bound to `pathname === '/'`
- JSX order in the nav flex container: /leaderboard Link, / Link, spacer div, /notifications Link, profile Link, Rate FAB
- Desktop Navbar (`src/components/layout/Navbar.tsx`) is untouched
- `npm run build` succeeds
</verification>

<success_criteria>
Bottom nav displays left-to-right: Home (bar chart) | Feed (house) | [FAB] | Alerts | Profile. The Home tab highlights on /leaderboard. The Feed tab highlights on /. No other tabs, routes, pages, or components are affected.
</success_criteria>

<output>
After completion, create `.planning/phases/11-nav-restructure/11-01-SUMMARY.md` summarizing what was changed, the final variable-to-route mapping, and confirmation that no other files were touched.
</output>
