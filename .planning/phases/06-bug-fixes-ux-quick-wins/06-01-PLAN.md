---
phase: 06-bug-fixes-ux-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/signup/page.tsx
  - src/app/settings/page.tsx
  - src/app/products/[slug]/page.tsx
  - src/app/flavors/[slug]/page.tsx
  - src/components/feed/FeedCard.tsx
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "User can set a username containing dots (e.g., 'john.doe') without validation errors on signup"
    - "User can set a username containing dots without validation errors on settings page"
    - "Email text is readable on light theme after signup (not white-on-white)"
    - "Taste/flavor tags display consistently -- shown when present, hidden when empty/null"
  artifacts:
    - path: "src/app/signup/page.tsx"
      provides: "Username regex accepts dots, email uses var(--text), hint mentions dots"
      contains: "/^[a-z0-9_.]+$/"
    - path: "src/app/settings/page.tsx"
      provides: "Username regex accepts dots, hint mentions dots"
      contains: "/^[a-z0-9_.]+$/"
  key_links:
    - from: "src/app/signup/page.tsx"
      to: "username validation"
      via: "regex test on form.username"
      pattern: "/\\^\\[a-z0-9_\\.\\]\\+\\$/"
    - from: "src/app/signup/page.tsx"
      to: "email display"
      via: "inline style with CSS variable"
      pattern: "var\\(--text\\)"
---

<objective>
Fix username validation to allow dots, fix email text visibility on light theme, and verify taste tag display consistency.

Purpose: Resolve three user-facing bugs (FIX-01, FIX-02, FIX-03) that affect signup/settings usability and theme readability.
Output: Updated signup and settings pages with correct validation and theme-aware styling.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bug-fixes-ux-quick-wins/06-CONTEXT.md
@.planning/phases/06-bug-fixes-ux-quick-wins/06-RESEARCH.md
@.planning/phases/06-bug-fixes-ux-quick-wins/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix username validation regex and hint text (FIX-01)</name>
  <files>src/app/signup/page.tsx, src/app/settings/page.tsx</files>
  <read_first>src/app/signup/page.tsx, src/app/settings/page.tsx</read_first>
  <action>
In `src/app/signup/page.tsx`:
- Line 25: Change regex from `/^[a-z0-9_]+$/` to `/^[a-z0-9_.]+$/`
- Line 26: Change error message from `'Username can only contain lowercase letters, numbers, and underscores.'` to `'Username can only contain lowercase letters, numbers, underscores, and dots.'`
- Line 77: Change hint prop from `"Lowercase letters, numbers, underscores only"` to `"Lowercase letters, numbers, underscores, and dots only"`

In `src/app/settings/page.tsx`:
- Line 45: Change regex from `/^[a-z0-9_]+$/` to `/^[a-z0-9_.]+$/`
- Line 46: Change error message from `'Username can only contain lowercase letters, numbers, and underscores.'` to `'Username can only contain lowercase letters, numbers, underscores, and dots.'`
- Line 193: Change hint prop from `"Lowercase letters, numbers, underscores only"` to `"Lowercase letters, numbers, underscores, and dots only"`

No database migration needed -- the `users` table has `TEXT UNIQUE NOT NULL` with no CHECK constraint on username characters.
  </action>
  <verify>
    <automated>grep -n "a-z0-9_\]" src/app/signup/page.tsx src/app/settings/page.tsx | grep -v "\." && echo "FAIL: old regex still present" || echo "PASS: all regexes updated"</automated>
  </verify>
  <acceptance_criteria>
- `src/app/signup/page.tsx` contains `/^[a-z0-9_.]+$/`
- `src/app/settings/page.tsx` contains `/^[a-z0-9_.]+$/`
- `src/app/signup/page.tsx` contains `"Lowercase letters, numbers, underscores, and dots only"`
- `src/app/settings/page.tsx` contains `"Lowercase letters, numbers, underscores, and dots only"`
- `src/app/signup/page.tsx` contains `underscores, and dots.`
- `src/app/settings/page.tsx` contains `underscores, and dots.`
- Zero occurrences of the old regex `/^[a-z0-9_]+$/` remain in either file
  </acceptance_criteria>
  <done>Username validation accepts dots in both signup and settings, hint text and error messages updated in both files</done>
</task>

<task type="auto">
  <name>Task 2: Fix email text visibility on light theme (FIX-02)</name>
  <files>src/app/signup/page.tsx</files>
  <read_first>src/app/signup/page.tsx</read_first>
  <action>
In `src/app/signup/page.tsx`, line 48, in the signup success screen:

Change:
```tsx
<span className="text-white font-medium">{form.email}</span>
```

To:
```tsx
<span style={{ color: 'var(--text)', fontWeight: 500 }}>{form.email}</span>
```

This replaces the hardcoded `text-white` Tailwind class (invisible on light theme) with a theme-aware CSS variable that renders correctly on all three themes (blue, light, black).
  </action>
  <verify>
    <automated>grep -n "text-white" src/app/signup/page.tsx && echo "FAIL: text-white still present" || echo "PASS: text-white removed"</automated>
  </verify>
  <acceptance_criteria>
- `src/app/signup/page.tsx` does NOT contain `text-white`
- `src/app/signup/page.tsx` contains `color: 'var(--text)'`
- `src/app/signup/page.tsx` contains `fontWeight: 500`
  </acceptance_criteria>
  <done>Email text uses theme-aware color variable, readable on all themes including light</done>
</task>

<task type="auto">
  <name>Task 3: Verify taste tag display consistency (FIX-03)</name>
  <files>src/app/products/[slug]/page.tsx, src/app/flavors/[slug]/page.tsx, src/components/feed/FeedCard.tsx</files>
  <read_first>src/app/products/[slug]/page.tsx, src/app/flavors/[slug]/page.tsx, src/components/feed/FeedCard.tsx, src/app/page.tsx</read_first>
  <action>
Verify that all four tag display surfaces use the correct guard pattern. Per research, all are already correct:

1. `src/app/products/[slug]/page.tsx` -- should have `flavor.tags && flavor.tags.length > 0 && (...)`
2. `src/app/flavors/[slug]/page.tsx` -- should have `flavor.tags && flavor.tags.length > 0 && (...)`
3. `src/components/feed/FeedCard.tsx` -- should have `ratingData.context_tags && ratingData.context_tags.length > 0 && (...)`
4. `src/app/page.tsx` -- should have `item.tags && item.tags.length > 0 && (...)`

If any surface is missing the guard, add it following the pattern: `{data.tags && data.tags.length > 0 && (<div style={{ display: 'flex', gap: '6px', flexWrap: 'wrap' }}>...</div>)}`.

If all guards are already in place (expected), confirm this in the summary -- no code change needed, requirement met by existing implementation.
  </action>
  <verify>
    <automated>grep -n "tags && .*tags.length > 0" src/app/products/\[slug\]/page.tsx src/app/flavors/\[slug\]/page.tsx src/components/feed/FeedCard.tsx src/app/page.tsx | wc -l | xargs -I {} bash -c 'if [ {} -ge 4 ]; then echo "PASS: all 4 tag guards present"; else echo "FAIL: found only {} guards"; fi'</automated>
  </verify>
  <acceptance_criteria>
- `src/app/products/[slug]/page.tsx` contains `flavor.tags && flavor.tags.length > 0`
- `src/app/flavors/[slug]/page.tsx` contains `flavor.tags && flavor.tags.length > 0`
- `src/components/feed/FeedCard.tsx` contains `context_tags && ratingData.context_tags.length > 0`
- `src/app/page.tsx` contains `item.tags && item.tags.length > 0`
  </acceptance_criteria>
  <done>All tag display surfaces consistently guard against empty/null tags -- tags shown when present, hidden when absent</done>
</task>

</tasks>

<verification>
- `grep -rn "^[a-z0-9_]+\$" src/app/signup/page.tsx src/app/settings/page.tsx` returns zero results (old regex gone)
- `grep -n "text-white" src/app/signup/page.tsx` returns zero results
- `grep -rn "tags.*length > 0" src/app/products/\[slug\]/page.tsx src/app/flavors/\[slug\]/page.tsx src/components/feed/FeedCard.tsx src/app/page.tsx` returns 4+ results
- `npx next build` completes without errors
</verification>

<success_criteria>
- Username "john.doe" passes client-side validation on both signup and settings pages
- Email text on signup success screen uses `var(--text)` CSS variable
- All tag display surfaces have null/empty guards
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fixes-ux-quick-wins/06-01-SUMMARY.md`
</output>
