---
phase: 08-comment-system-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/rating/CommentsSection.tsx
autonomous: false
requirements:
  - COMM-01
  - COMM-02
  - COMM-03

must_haves:
  truths:
    - "User can tap three-dot menu or long-press their own comment to see Edit and Delete options"
    - "User can edit their comment inline and the comment shows 'edited' marker after save"
    - "User can delete their comment -- immediate removal if no replies, '[Comment deleted]' placeholder if replies exist"
    - "User can tap Reply on a top-level comment and post a threaded reply"
    - "Replies appear indented under the parent with left accent border and smaller avatars"
    - "Reply button does NOT appear on reply comments (single level only)"
    - "Edit/reply/replyingTo state resets when sheet closes"
  artifacts:
    - path: "src/components/rating/CommentsSection.tsx"
      provides: "Full comment UI with edit, delete, threading"
      min_lines: 500
  key_links:
    - from: "src/components/rating/CommentsSection.tsx"
      to: "review_comments table"
      via: "Supabase UPDATE for edit and soft-delete"
      pattern: "from.*review_comments.*update"
    - from: "src/components/rating/CommentsSection.tsx"
      to: "review_comments table"
      via: "Supabase INSERT with parent_comment_id for replies"
      pattern: "parent_comment_id"
    - from: "src/components/rating/CommentsSection.tsx"
      to: "review_comments table"
      via: "Fetch includes new columns for threading/delete/edit display"
      pattern: "is_deleted.*edited_at"
---

<objective>
Implement edit, delete, and single-level threaded reply UI in CommentsSection.tsx. This is the core feature work for all three COMM requirements.

Purpose: Users can manage their comments (edit/delete) and engage in threaded conversations (replies), all within the existing bottom sheet.
Output: Fully functional comment system with edit markers, soft delete placeholders, and Instagram-style single-level threading.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-comment-system-upgrade/08-CONTEXT.md
@.planning/phases/08-comment-system-upgrade/08-RESEARCH.md
@.planning/phases/08-comment-system-upgrade/08-01-SUMMARY.md

<interfaces>
<!-- Updated ReviewComment interface (from Plan 01): -->
export interface ReviewComment {
  id: string
  rating_id: string
  user_id: string
  text: string | null
  parent_comment_id: string | null
  is_deleted: boolean
  edited_at: string | null
  created_at: string
}

<!-- Current Comment interface in CommentsSection.tsx (will be updated): -->
interface Comment {
  id: string
  text: string
  created_at: string
  user: { username: string; avatar_url: string | null } | null
}

<!-- Established patterns from CommentsSection.tsx: -->
- Inline styles with CSS variables: var(--bg-card), var(--text), var(--accent), var(--border), var(--text-dim), var(--text-faint), var(--bg-elevated), var(--border-soft), var(--text-muted)
- minHeight: '44px' touch targets
- Optimistic UI via loadComments() after mutations
- Client-side Supabase calls via useMemo(() => createClient(), [])
- createPortal to document.body
- timeAgo() for timestamps
- Avatar: 28px with Image or initials fallback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edit/delete functionality with three-dot menu and long-press</name>
  <files>src/components/rating/CommentsSection.tsx</files>
  <read_first>
    src/components/rating/CommentsSection.tsx
    src/lib/types.ts
    .planning/phases/08-comment-system-upgrade/08-CONTEXT.md
    .planning/phases/08-comment-system-upgrade/08-RESEARCH.md
  </read_first>
  <action>
Modify `CommentsSection.tsx` to add edit and delete capabilities. All changes are within this single file. Keep all existing inline style patterns (CSS variables, 44px touch targets, no Tailwind).

**1. Update the local Comment interface** to include the new fields:
```typescript
interface Comment {
  id: string
  text: string | null
  created_at: string
  user_id: string  // needed for ownership check
  parent_comment_id: string | null
  is_deleted: boolean
  edited_at: string | null
  user: { username: string; avatar_url: string | null } | null
}
```

**2. Update `loadComments`** to fetch the new columns:
- Change `.select('id, text, created_at, user_id')` to `.select('id, text, created_at, user_id, parent_comment_id, is_deleted, edited_at')`
- Remove the `.limit(20)` (we now need all comments to group replies; the 20-limit will be applied client-side to top-level only)
- Map the new fields into the Comment objects: `parent_comment_id: c.parent_comment_id, is_deleted: c.is_deleted, edited_at: c.edited_at, user_id: c.user_id`

**3. Add state variables** inside `CommentBottomSheet`:
```typescript
const [menuOpenFor, setMenuOpenFor] = useState<string | null>(null)
const [editingId, setEditingId] = useState<string | null>(null)
const [editText, setEditText] = useState('')
const [confirmDeleteId, setConfirmDeleteId] = useState<string | null>(null)
const longPressRef = useRef<ReturnType<typeof setTimeout> | null>(null)
const touchStartPos = useRef<{ x: number; y: number } | null>(null)
```

**4. Reset state on sheet close** -- add to the existing `useEffect` that watches `open`, or add a new one:
```typescript
useEffect(() => {
  if (!open) {
    setMenuOpenFor(null)
    setEditingId(null)
    setEditText('')
    setConfirmDeleteId(null)
  }
}, [open])
```

**5. Add long-press handlers** (functions inside the component):
- `onTouchStart(e: React.TouchEvent, commentId: string)`: Store touch position in `touchStartPos.current`, set `longPressRef.current = setTimeout(() => setMenuOpenFor(commentId), 500)`
- `onTouchMove(e: React.TouchEvent)`: If touch moved > 10px from start, clear the timeout
- `onTouchEnd()`: Clear the timeout
- Cancel long-press if touch moves (prevents conflict with scroll per research pitfall 5)

**6. Add three-dot menu (kebab) button** on each comment row -- only visible when `comment.user_id === user?.id` (current user owns the comment) AND `!comment.is_deleted`:
- Position: absolute, top-right of the comment bubble
- Icon: vertical ellipsis (three dots) using simple spans or SVG
- On click: `setMenuOpenFor(comment.id)` (toggle)
- Menu: a small dropdown below the button with two options:
  - "Edit" -- sets `setEditingId(comment.id)` and `setEditText(comment.text ?? '')` and `setMenuOpenFor(null)`
  - "Delete" -- sets `setConfirmDeleteId(comment.id)` and `setMenuOpenFor(null)`
- Style: `background: var(--bg-card)`, `border: 1px solid var(--border)`, `borderRadius: '8px'`, `boxShadow: '0 2px 8px rgba(0,0,0,0.15)'`, `zIndex: 10`, position absolute
- Click outside the menu (or tapping anything else) closes it: use a click handler on the backdrop or check in the comment list click

**7. Inline edit mode** -- when `editingId === comment.id`:
- Replace the comment text div with a `<textarea>` pre-filled with `editText`
- Style: same background as comment bubble (`var(--bg-elevated)`), border `1px solid var(--accent)`, borderRadius `8px`, padding `7px 10px`, fontSize `13px`, color `var(--text)`, width `100%`, fontFamily `inherit`, resize `none`
- Below the textarea, two buttons: "Save" (accent background, dark text) and "Cancel" (transparent, text-dim)
- Both buttons: `minHeight: '32px'`, `fontSize: '12px'`, `fontWeight: 600`, `borderRadius: '16px'`, `padding: '4px 12px'`
- Save handler: calls `db.from('review_comments').update({ text: editText.trim(), edited_at: new Date().toISOString() }).eq('id', comment.id)`, then `setEditingId(null)`, then `loadComments()`
- Cancel handler: `setEditingId(null)`

**8. Delete confirmation** -- when `confirmDeleteId === comment.id`:
- Show inline confirmation below the comment: "Delete this comment?" with "Delete" (red text) and "Cancel" buttons
- Style: `fontSize: '12px'`, same layout as edit buttons
- Delete handler logic (per user decision):
  - Check if comment has replies: `const hasReplies = comments.some(c => c.parent_comment_id === comment.id && !c.is_deleted)`
  - If `hasReplies`: soft delete -- `db.from('review_comments').update({ is_deleted: true, text: null }).eq('id', comment.id)`
  - If `!hasReplies`: hard delete -- `db.from('review_comments').delete().eq('id', comment.id)`
  - After either: `setConfirmDeleteId(null)`, then `loadComments()`

**9. "edited" marker** -- when `comment.edited_at` is not null and `!comment.is_deleted`:
- Append ` . edited` after the `timeAgo(comment.created_at)` text
- Style: same as timestamp span -- `fontSize: '11px'`, `color: 'var(--text-faint)'`, `fontWeight: 400`
- Use a middle dot separator: `{comment.edited_at && ' \u00b7 edited'}`

**10. Deleted comment placeholder** -- when `comment.is_deleted`:
- Replace the entire comment bubble content with `[Comment deleted]`
- Style: `fontStyle: 'italic'`, `color: 'var(--text-faint)'`, `fontSize: '13px'`
- Do NOT show username, avatar link, three-dot menu, or Reply button on deleted comments
- Still render the comment row (so its replies appear below it)

**11. Close menu on outside click**: Add an `onClick` handler to the scrollable comments container that sets `setMenuOpenFor(null)` when clicking outside a menu.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `CommentsSection.tsx` Comment interface contains `user_id: string`, `parent_comment_id: string | null`, `is_deleted: boolean`, `edited_at: string | null`
    - `loadComments` select string includes `parent_comment_id, is_deleted, edited_at`
    - State variables exist: `menuOpenFor`, `editingId`, `editText`, `confirmDeleteId`
    - `useEffect` resets `editingId`, `menuOpenFor`, `confirmDeleteId` when `open` is false
    - Long-press handlers exist: functions referencing `longPressRef`, `touchStartPos`, `setTimeout` with 500ms
    - Three-dot button rendered conditionally: `comment.user_id === user?.id` check present
    - Inline edit: textarea element present when `editingId === comment.id`
    - Edit save calls `.update({ text:` and `edited_at:` on `review_comments`
    - Delete handler checks `hasReplies` and conditionally uses `.update({ is_deleted: true, text: null })` vs `.delete()`
    - "edited" marker: string `edited` rendered conditionally on `comment.edited_at`
    - Deleted placeholder: string `[Comment deleted]` rendered when `comment.is_deleted`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Edit and delete functionality fully implemented: three-dot menu + long-press trigger, inline edit with Save/Cancel and "edited" marker, delete with confirmation (soft delete for comments with replies, hard delete otherwise), deleted placeholder rendering</done>
</task>

<task type="auto">
  <name>Task 2: Add single-level threaded reply UI with reply mode, indented display, and load-more</name>
  <files>src/components/rating/CommentsSection.tsx</files>
  <read_first>
    src/components/rating/CommentsSection.tsx
    .planning/phases/08-comment-system-upgrade/08-CONTEXT.md
  </read_first>
  <action>
Continue modifying `CommentsSection.tsx` to add reply threading. Build on the edit/delete work from Task 1. All inline styles with CSS variables, no Tailwind.

**1. Add reply state variables** (if not already added in Task 1):
```typescript
const [replyingTo, setReplyingTo] = useState<{ commentId: string; username: string } | null>(null)
const [expandedReplies, setExpandedReplies] = useState<Set<string>>(new Set())
```
Also reset `replyingTo` when `open` changes to false (in the existing reset useEffect).

**2. Client-side comment grouping** -- after `loadComments` populates the comments state, derive grouped data for rendering. Add a `useMemo` that computes:
```typescript
const { topLevelComments, repliesByParent } = useMemo(() => {
  const topLevel = comments
    .filter(c => !c.parent_comment_id)
    .slice(0, 20)  // 20 top-level limit per user decision
  const repliesByParent: Record<string, Comment[]> = {}
  for (const c of comments.filter(c => c.parent_comment_id)) {
    const pid = c.parent_comment_id!
    if (!repliesByParent[pid]) repliesByParent[pid] = []
    repliesByParent[pid].push(c)
  }
  return { topLevelComments: topLevel, repliesByParent }
}, [comments])
```

**3. Update the comment list rendering** to iterate over `topLevelComments` instead of `comments`, and render replies underneath each top-level comment:

For each top-level comment:
- Render the existing comment row (with edit/delete/edited from Task 1)
- Below the comment bubble (after the bubble div, inside the flex-1 wrapper), add a "Reply" text button:
  - Text: `Reply`
  - Style: `background: 'none'`, `border: 'none'`, `cursor: 'pointer'`, `color: 'var(--text-faint)'`, `fontSize: '11px'`, `fontWeight: 600`, `padding: '4px 0'`, `marginTop: '2px'`, `minHeight: '44px'` (touch target)
  - On click: `setReplyingTo({ commentId: comment.id, username: comment.user?.username ?? 'anon' })`, then focus the input field
  - Do NOT render Reply button on deleted comments (`comment.is_deleted`)
- After the top-level comment row, render its replies from `repliesByParent[comment.id]`:
  - Show first 5 replies by default
  - If more than 5 exist and `!expandedReplies.has(comment.id)`, show a "View N more replies" link:
    - Text: `View ${repliesByParent[comment.id].length - 5} more replies`
    - Style: `color: 'var(--text-faint)'`, `fontSize: '12px'`, `fontWeight: 600`, `cursor: 'pointer'`, `padding: '4px 0 4px 44px'` (indented to align with reply content), `minHeight: '44px'`
    - On click: `setExpandedReplies(prev => new Set(prev).add(comment.id))`
  - If `expandedReplies.has(comment.id)`, show ALL replies

**4. Reply comment row styling** -- each reply is rendered with:
- Container: `marginLeft: '24px'` (indent), `borderLeft: '2px solid var(--accent)'`, `paddingLeft: '12px'`, `marginBottom: '10px'`
- Avatar: `width: '22px'`, `height: '22px'` (smaller than 28px top-level), `fontSize: '9px'`
- Image component: `width={22} height={22}`
- Same bubble style as top-level (bg-elevated, 10px borderRadius, 7px 10px padding)
- Reply comments do NOT have a "Reply" button (single level only per user decision -- replies cannot be replied to)
- Reply comments still get three-dot menu + long-press if owned by current user (edit/delete works on replies too)

**5. Reply mode chip** -- when `replyingTo` is not null, show a dismissable chip between the scrollable comments list and the sticky input:
- Position: inside the sticky footer area, above the form, below the border-top
- Style: `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'space-between'`, `padding: '6px 16px'`, `fontSize: '12px'`, `color: 'var(--text-dim)'`, `background: 'var(--bg-elevated)'`
- Content: `Replying to @{replyingTo.username}`
- Dismiss button: small X icon, `minWidth: '32px'`, `minHeight: '32px'`, on click: `setReplyingTo(null)`

**6. Update handleSubmit** to include `parent_comment_id` when in reply mode:
```typescript
const insertPayload: { rating_id: string; user_id: string; text: string; parent_comment_id?: string } = {
  rating_id: ratingId,
  user_id: user.id,
  text: text.trim().slice(0, 280),
}
if (replyingTo) {
  insertPayload.parent_comment_id = replyingTo.commentId
}
const { error } = await db.from('review_comments').insert(insertPayload)
```
After successful submit, also `setReplyingTo(null)`.

**7. Focus input on Reply tap**: Add a ref to the input element (`inputRef = useRef<HTMLInputElement>(null)`), and in the Reply button onClick, call `inputRef.current?.focus()` after setting replyingTo.

**8. Empty state update**: The "No comments yet" empty state should check `topLevelComments.length === 0` instead of `comments.length === 0`.

**9. Comment count update on reply**: The `onCommentPosted` callback should fire for both top-level comments AND replies, so the feed card count increments correctly.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `replyingTo` state variable exists with type `{ commentId: string; username: string } | null`
    - `expandedReplies` state variable exists with type `Set<string>`
    - `useMemo` groups comments into `topLevelComments` and `repliesByParent`
    - Top-level comments limited to 20 via `.slice(0, 20)`
    - Reply button rendered on top-level non-deleted comments with text `Reply`
    - Reply button does NOT render on reply comments (check: `!comment.parent_comment_id`)
    - Reply rows have `marginLeft: '24px'` and `borderLeft:` containing `var(--accent)`
    - Reply avatars use `width: '22px'`, `height: '22px'`
    - "View N more replies" link renders when replies exceed 5 and parent not in expandedReplies
    - Reply mode chip contains `Replying to @` and dismiss X button
    - `handleSubmit` insert payload conditionally includes `parent_comment_id` from `replyingTo.commentId`
    - `replyingTo` cleared after successful submit (`setReplyingTo(null)`)
    - `replyingTo` cleared when sheet closes (in reset useEffect)
    - Input ref exists and `focus()` called on Reply tap
    - `npx tsc --noEmit` exits 0
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Reply threading fully implemented: Reply button on top-level comments, reply mode chip with dismiss, replies indented with accent border and smaller avatars, View N more replies link, single-level enforcement (no Reply on replies), parent_comment_id sent on submit, full build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete comment system upgrade</name>
  <files>src/components/rating/CommentsSection.tsx</files>
  <action>
Human verification of the complete comment system. No code changes -- this is a visual and functional check of all COMM requirements in the browser.
  </action>
  <what-built>
    Complete comment system with edit, delete, and single-level threaded replies in the existing bottom sheet.
  </what-built>
  <how-to-verify>
    1. Open the app and navigate to a review with comments
    2. **Edit test**: Tap the three-dot menu (or long-press) on your own comment. Tap Edit. Verify the comment bubble transforms into an editable text field. Change text, tap Save. Verify the comment updates and shows "edited" after the timestamp.
    3. **Delete test (no replies)**: Post a new comment (no replies on it). Tap three-dot menu, Delete. Confirm. Verify comment disappears immediately.
    4. **Delete test (with replies)**: Find or create a comment with replies. Delete the parent. Verify it shows "[Comment deleted]" in italic/dim text, with replies still visible underneath.
    5. **Reply test**: Tap "Reply" on a top-level comment. Verify "Replying to @username X" chip appears above the input. Type a reply and post. Verify reply appears indented under the parent with a left accent border and smaller avatar.
    6. **Single-level check**: Verify reply comments do NOT have a Reply button (only top-level comments do).
    7. **Owner-only check**: View another user's comment. Verify no three-dot menu appears.
    8. **Sheet close reset**: Start editing a comment, close the sheet, reopen. Verify edit mode is not persisted.
    9. **Feed count check**: Verify comment count on feed cards matches actual visible comments (deleted comments should not be counted).
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 9 verification steps pass in browser
    - No TypeScript errors
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>All COMM-01, COMM-02, COMM-03 requirements verified by human in browser</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx next build` succeeds
- CommentsSection.tsx contains edit, delete, and reply functionality
- All three COMM requirements verifiable in browser
</verification>

<success_criteria>
- Edit: three-dot menu + long-press, inline edit, "edited" marker, Save/Cancel
- Delete: confirmation dialog, hard delete (no replies) or soft delete with "[Comment deleted]" placeholder (has replies)
- Reply: Reply button on top-level only, reply mode chip, indented reply display with accent border, 5-reply limit with "View more"
- TypeScript compiles, Next.js builds, all UI renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-comment-system-upgrade/08-02-SUMMARY.md`
</output>
